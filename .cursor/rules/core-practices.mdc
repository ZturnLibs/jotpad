---
description: Jotpad 核心编码实践：单一职责、组合扩展、代码注释约束
alwaysApply: true
---

# 核心实践

## 单一职责

- 每个函数只做一件事；超过一个职责就拆分。
- 每个组件只负责一种 UI 关注点（展示 / 交互壳 / 数据桥接择一）。
- Store action 按领域拆分（tabs / file / settings），避免「全能」方法。

## 组合优于继承与巨型组件

- 用小组件组合扩展能力，不要堆 props / 条件分支做成上帝组件。
- 可复用逻辑抽到 `src/lib/` 或自定义 hook，组件保持薄。

```tsx
// ❌ BAD：一个组件里塞查找、状态栏、设置
function EditorApp() { /* find + status + settings + editor */ }

// ✅ GOOD：组合
<>
  <Toolbar />
  <Editor />
  <StatusBar />
  {findOpen && <FindBar />}
</>
```

## 代码注释（强制）

优先把意图写进命名与结构；注释只补代码读不出来的信息。语言：**简洁中文**（专有名词、错误码、API 名可保留英文原文）。

### 必须写（缺则视为未完成）

- **为什么**：业务规则、平台差异、编码/BOM/行尾/草稿策略、与系统/原生能力的取舍
- **不变量与边界**：状态机、副作用时序、并发/竞态、安全或权限相关约束
- **对外契约**：导出模块文件头一行用途；非平凡公共 API / 重要类型的边界与失败语义
- **坑与临时约束**：已知缺陷、上游行为怪癖、错误码含义（如 `voice:nospeech`）、刻意不处理的分支
- **改代码时同步改注释**；过时注释比没有更糟，发现即删或改准

### 禁止写

- 复述显而易见的代码（赋值、平凡 JSX、一眼能懂的分支）
- 纯英文「翻译式」注释，或无信息的 `// fix` / `// temp`
- 大段注释掉的死代码（应删除；需要痕迹用 git）
- 用注释代替拆分：逻辑已难读时先重构，再补必要的「为什么」

```ts
// ❌ BAD：复述 / 无信息
// 增加 i
i++;
// fix
handleError(e);

// ✅ GOOD：意图、约束、边界
// 未保存草稿按标签 ID 落盘；路径为空时视为「新建」不写文件名
// sherpa 常把结果 JSON 打到 stderr，须与 stdout 合并后再解析
```

域内补充见 `typescript-lib.mdc` / `tauri-rust.mdc` / `react-composition.mdc`；日志与错误码见 `logging.mdc`。

## 通用质量

- 改动保持最小范围；不顺手重构无关代码。
- 用户可见文案走 `src/lib/i18n.ts`，不硬编码中英字符串。
- 优先复用现有 `src/lib/*`、`useStore`、Tauri backend，避免平行实现。
