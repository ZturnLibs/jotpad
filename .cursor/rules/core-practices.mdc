---
description: Jotpad 核心编码实践：单一职责、组合扩展、中文注释
alwaysApply: true
---

# 核心实践

## 单一职责

- 每个函数只做一件事；超过一个职责就拆分。
- 每个组件只负责一种 UI 关注点（展示 / 交互壳 / 数据桥接择一）。
- Store action 按领域拆分（tabs / file / settings），避免「全能」方法。

## 组合优于继承与巨型组件

- 用小组件组合扩展能力，不要堆 props / 条件分支做成上帝组件。
- 可复用逻辑抽到 `src/lib/` 或自定义 hook，组件保持薄。

```tsx
// ❌ BAD：一个组件里塞查找、状态栏、设置
function EditorApp() { /* find + status + settings + editor */ }

// ✅ GOOD：组合
<>
  <Toolbar />
  <Editor />
  <StatusBar />
  {findOpen && <FindBar />}
</>
```

## 中文注释（必要处必须写）

在以下位置使用简洁中文注释，说明「为什么」而非复述代码：

- 非显而易见的业务规则、平台差异、编码/行尾/草稿策略
- 复杂算法、状态机、副作用时序
- 对外导出的模块 / 重要类型 / 非平凡公共 API
- 临时约束、已知坑、与原生/系统能力相关的取舍

```ts
// ❌ BAD：无信息或纯英文复述
// increment i
i++;

// ✅ GOOD：解释意图与约束
// 未保存草稿按标签 ID 落盘；路径为空时视为「新建」不写文件名
```

不要给简单赋值、平凡 JSX、一眼能懂的代码加注释。

## 通用质量

- 改动保持最小范围；不顺手重构无关代码。
- 用户可见文案走 `src/lib/i18n.ts`，不硬编码中英字符串。
- 优先复用现有 `src/lib/*`、`useStore`、Tauri backend，避免平行实现。
