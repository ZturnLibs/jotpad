# Jotpad 功能路线图

> 定位：跨平台**纯文本**记事本（比系统记事本强、比 IDE/笔记应用轻）。  
> 原则：系统已有能力优先走原生；不默认做成 Notion / Word / VS Code。  
> 更新日期：2026-07-28

---

## 当前已具备（基线）

| 能力 | 说明 |
| --- | --- |
| 多标签 + 侧栏文件列表 | 左右分栏，中键关闭、右键菜单 |
| 草稿自动保存 / 恢复 | 未保存内容可恢复 |
| 多编码 + 行尾 | UTF-8/GBK 等，BOM、CRLF/LF/CR |
| 查找 / 替换 / 转到行 | 含正则、匹配计数 |
| 外部文件变更检测 | 提示重新加载或保留 |
| 字体 / 缩放 / 换行 / 行号 / 主题 | 含强调色、中英界面 |
| 打印与页面设置 | |
| 系统集成 | 可选「新建文本文件」「用 Jotpad 打开」 |
| 文件操作 | 重命名、复制路径、访达/资源管理器定位、删除 |

---

## 版本规划总览

| 阶段 | 主题 | 目标用户价值 | 粗估 |
| --- | --- | --- | --- |
| **v0.2** | 职场提效小刀 | 开会能对照、文案能统计/纠错 | 1–2 周 |
| **v0.3** | 工作现场可恢复 | 「昨天那组文件」一键回来 | 1–2 周 |
| **v0.4** | 批量改字与大文件 | 名单/日志/配置批量处理更稳 | 2–3 周 |
| **v0.5** | 可选结构化 | 需要时像轻量 Markdown，默认仍是 txt | 2–3 周 |
| **以后** | 战略项 | AI / 云同步等，需单独定定位后再做 | — |

---

## v0.2 — 职场提效（P0）

**目标**：不改产品基因，先补开会与写文案最高频缺口。

| # | 功能 | 说明 | 优先级 | 依赖/风险 |
| --- | --- | --- | --- | --- |
| 1 | **窗口置顶** | 菜单/快捷键切换 Always on Top | P0 | ✅ `Cmd/Ctrl+Shift+T`、查看菜单、状态栏 |
| 2 | **增强统计** | 状态栏：字符/词/行；选区时显示选区统计 | P0 | ✅ 含 CJK 词统计 |
| 3 | **拼写检查（可关）** | 系统词典；设置/查看菜单开关 | P0 | ✅ 默认关，开启后走 WebView spellcheck |
| 4 | **侧栏搜索已打开文件** | 按文件名过滤标签列表 | P0 | ✅ ≥2 个标签时显示筛选框 |
| 5 | **固定/收藏标签** | 常用文件钉在侧栏顶部 | P1 | 持久化到 state |

**验收**：开会时置顶记要点；写限字数文案能一眼看到字数；拼写错误有提示且可关闭。

---

## v0.3 — 会话与找回（P1 前半）

**目标**：支持「一组相关文件」作为工作单元，而不只是单文件草稿。

| # | 功能 | 说明 | 优先级 | 依赖/风险 |
| --- | --- | --- | --- | --- |
| 6 | **命名会话** | 保存/打开「当前打开的文件列表 + 活动标签」 | P0 | ✅ 仅含磁盘文件；同名覆盖 |
| 7 | **启动时恢复上次会话** | 设置：恢复标签 / 空白起步 | P0 | ✅ `startupMode` |
| 8 | **最近会话列表** | 文件菜单「最近会话」 | P1 | ✅ 随命名会话一并完成 |
| 9 | **全局快速打开** | 已打开 + 最近文件的模糊搜索（`Cmd/Ctrl+P`） | P1 | ✅ 打印改为仅菜单 |

**验收**：关掉应用再开，可选恢复昨天的会议纪要相关文件组。

---

## v0.4 — 批量编辑与稳健性（P1 后半）

**目标**：服务改配置、名单、日志的办公/技术混合用户。

| # | 功能 | 说明 | 优先级 | 依赖/风险 |
| --- | --- | --- | --- | --- |
| 10 | **多光标** | Ctrl/Cmd+点击；选相同词 | P0 | CodeMirror 已有基础 API |
| 11 | **列（矩形）选择** | Alt 拖选改前缀/对齐列 | P1 | 交互与快捷键冲突梳理 |
| 12 | **大文件策略** | 超阈值提示只读/分块；保证不卡死 | P0 | 需实测阈值门槛 |
| 13 | **只读模式** | 防误改日志；标题栏标识 | P1 | |
| 14 | **与磁盘版本对比** | 轻量 diff（当前缓冲 vs 文件） | P2 | UI 复杂度 |

**验收**：打开数 MB 日志不卡死；批量改一列前缀不用另开 Excel。

---

## v0.5 — 可选 Markdown（P2 / 战略分叉）

**目标**：承接 WordPad 退场后的「轻结构」需求，但**默认仍是纯文本**。

| # | 功能 | 说明 | 优先级 | 依赖/风险 |
| --- | --- | --- | --- | --- |
| 15 | **Markdown 语法高亮** | `.md` 或手动开启 | P1 | 保持文件仍为纯文本 |
| 16 | **预览面板（可关）** | 分屏预览，非所见即所得强绑 | P1 | |
| 17 | **格式工具栏（可选）** | 粗体/标题/列表 → 插入 Markdown 标记 | P2 | 易与「纯记事本」人设冲突，须可关 |

**决策点**：若反馈强烈要求富文本/插图，应单独立项评估，而不是塞进 v0.5。

---

## 暂缓 / 不做（除非改定位）

| 项 | 原因 |
| --- | --- |
| 云同步 / 多端协作 | 变成笔记产品，账号与合规成本高 |
| 内置 AI 改写/摘要（默认） | 订阅、隐私、国内网络；可作以后可选插件 |
| 完整富文本、图片、附件 | 偏离 txt；与系统备忘录/Word 重叠 |
| 重度待办 / 日历提醒 | 更适合独立效率应用 |
| 完整 IDE（LSP、调试） | 体积与复杂度爆炸 |

---

## 平台与分发（穿插各版本）

与功能版本并行，按需插入：

- [ ] 默认关联 `.txt` / 「打开方式」体验打磨（现有 shell 集成加固）
- [ ] 系统托盘 + 全局唤起快捷键（偏 v0.3）
- [ ] 便携版 / 静默安装说明（企业场景）
- [ ] 各平台安装包与更新通道

---

## 建议执行顺序（最近 4–6 周）

```
v0.2.1  窗口置顶 + 选区/全文统计          ✅ 已完成（2026-07-28）
v0.2.2  拼写检查开关 + 侧栏文件名过滤      ✅ 已完成（2026-07-28）
v0.3.0  命名会话 + 启动恢复策略            ✅ 已完成（2026-07-28）
v0.3.1  快速打开（已打开/最近）            ✅ 已完成（2026-07-28）
v0.4.x  多光标 → 大文件 → 只读
v0.5    仅在用户明确要「轻 Markdown」后再排期
```

---

## 成功指标（产品侧，可选跟踪）

- 日活中置顶功能使用率（开会场景代理指标）
- 「恢复会话」开启比例
- 因编码/卡顿相关的差评或 issue 数量下降
- 保持「启动到可输入」体感仍明显快于 IDE

---

## 备注

- 工作量粗估按 **1 名熟悉本仓库的全栈** 计，含自测，不含商店审核。
- 每项上线前保持：纯文本默认路径不被打断；新能力尽量可关。
